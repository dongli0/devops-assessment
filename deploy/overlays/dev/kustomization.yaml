apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: portfolio-dev

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: api-environment-patch.yaml

labels:
  - pairs:
      app.kubernetes.io/instance: portfolio-dev
      environment: dev
    includeSelectors: false
    includeTemplates: true
