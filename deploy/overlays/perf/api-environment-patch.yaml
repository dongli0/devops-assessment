apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: PORTFOLIO_ENVIRONMENT
              value: perf
          startupProbe:
            httpGet:
              path: /perf/api/health/live
          livenessProbe:
            httpGet:
              path: /perf/api/health/live
          readinessProbe:
            httpGet:
              path: /perf/api/health/ready
