apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: PORTFOLIO_ENVIRONMENT
              value: production
          startupProbe:
            httpGet:
              path: /production/api/health/live
          livenessProbe:
            httpGet:
              path: /production/api/health/live
          readinessProbe:
            httpGet:
              path: /production/api/health/ready
